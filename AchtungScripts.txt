ssh-agent bash		#starts agent
eval `ssh-agent -s`	
echo "$SSH_AUTH_SOCK"	#ensures socket is on

#ssh -i .ssh/achtungKey jsradford@achtung.ccs.neu.edu
ssh achtung #agent has been automated in .config

#add conda to path, do this for achtung and achtungXX
PATH=/home/jsradford/anaconda2/bin:$PATH

#Now Connected

upload 
scp -r -i ~/.ssh/achtungKey [filename] jsradford@achtung.neu.edu:/home/jsradford/

[on achtung08] $ ipython notebook
[on achtung] $ ssh -N -f -L localhost:7902:localhost:8888 achtung07
[on local host] $ ssh -N -f -L localhost:8080:localhost:7902 achtung


#set up tmux
tmux new -s SOME-NAEM


#Steps for setting up tmux:
[local]ssh achtung
[achtung] tmux new -s jason1
[achtung-jason1] ssh -N -f -L localhost:7902:localhost:8888 achtung08
[achtung-jason1] ssh achtung08
[achtung08-jason1] PATH=/home/jsradford/anaconda2/bin:$PATH
[achtung08-jason1] ipython notebook --no-browser
[achtung08-jason1] tmux detach

[local] ssh -N -f -L localhost:8080:localhost:7902 achtung

#to kill tmux
[achtung] tmux ls   #list all sessions, find the one to kill
[achtung] tmux -kill-session -t <session-name>


#RUNNING A SCRIPT
conda info --envs   #lists created environments
source activate <environ>
python /home/<location/filename.py

#if packages are missing:
conda install --name <environ> <package>

#to enter python directly:
python


